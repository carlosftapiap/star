'use server';

// This is a server action, it cannot access localStorage.
// The logout logic will be handled on the client side.
// We keep this file for potential future server actions.
// For now, the button in the UI will handle logout.
export async function logout() {
  // This function is now a placeholder.
  // The actual redirection is handled by the client.
}
